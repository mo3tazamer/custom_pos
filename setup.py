from setuptools import setup, find_packages

setup(
    name='custom_pos',
    version='1.0.0',
    description='Custom POS for ERPNext v16 - Vue 3',
    author='Your Company',
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True
)
